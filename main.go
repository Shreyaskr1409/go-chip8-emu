package main

func main() {
    // setup graphics
    // setup inputs

    // initialize registers of chip 8
    // load the game into the memory

    // start the emulation loop which will be endless
    // LOOP START:
    //    emulate one cycle
    //    if draw flag is set then update the screen
    //    pass key state (Key presses)
    // LOOP END
}
